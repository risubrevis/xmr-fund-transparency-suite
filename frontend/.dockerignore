# Build output — regenerated by `vite build` inside the build stage.
dist/

# Dependencies — installed inside the image via `npm install`.
node_modules/

# Vite cache
.vite/

# VCS & editor metadata
.git/
.gitignore
.zed/