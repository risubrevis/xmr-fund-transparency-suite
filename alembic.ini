[alembic]
script_location = backend/alembic
# Overridden by backend/alembic/env.py from DATABASE_URL environment variable at runtime.
# Uses psycopg2 (synchronous driver) because Alembic does not support async drivers.
sqlalchemy.url = postgresql+psycopg2://xmruser:changeme@localhost:5432/xmrdashboard
